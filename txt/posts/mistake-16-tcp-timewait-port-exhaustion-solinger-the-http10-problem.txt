@title Mistake 16: TCP TIME_WAIT Port Exhaustion & SO_LINGER The HTTP/1.0 Problem :: Rahul's Systems Blog
==============================================================================================
  RAHUL'S SYSTEMS BLOG -- systems programming, worked out by hand                     est. 2026
==============================================================================================
  [home](../index.txt) | [about](../about.txt) | [archive](../archive.txt) | [glossary](../glossary.txt) | [contact](mailto:learn.rahul.rai@gmail.com)
----------------------------------------------------------------------------------------------

  SPECIAL -- IPC MISTAKE: TCP TIME_WAIT PORT EXHAUSTION & SO_LINGER THE HTTP/1.0 PROBLEM
  Mistake 16: TCP TIME_WAIT Port Exhaustion & SO_LINGER The HTTP/1.0 Problem
  ============================================================================================


  When the server closes a socket, the OS kernel stubbornly hoards the socket in RAM in the `TIME_WAIT` state for 2-4 minutes to catch delayed packets. In the 90s, HTTP/1.0 servers (which closed every connection) would crash after hoarding 65,535 ports. Setting `SO_LINGER` with `l_linger = 0` forces the kernel to send an aggressive `RST` packet, instantly vaporizing the connection from RAM and bypassing `TIME_WAIT` entirely.

  --------
  DATAFLOW AND PROCESS CONSTRAINTS

```text
Normal Close: close(fd) ---> Sends FIN ---> enters TIME_WAIT (Kernel hoards port/RAM for 2-4 mins)
Linger Close: SO_LINGER (l_onoff=1, l_linger=0) ---> Sends RST ---> vaporizes connection instantly
```

  --------
  WE INSPECT THE TARGET SYSTEM CODE

```text
#include <stdio.h>
#include <iostream>
#include <cstring>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <signal.h>
#include <netinet/tcp.h>

void support() {
    int server_fd = socket(AF_INET, SOCK_STREAM, 0);
    int opt = 1; 
    setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, (const char*)&opt, sizeof(opt)); 
    
    struct sockaddr_in address;
    address.sin_family = AF_INET;
    address.sin_addr.s_addr = INADDR_ANY;
    address.sin_port = htons(8080);
    
    bind(server_fd, (struct sockaddr*)&address, sizeof(address));
    listen(server_fd, 100);
    
    int client_fd = accept(server_fd, nullptr, nullptr);
    
    int flag = 1;
    setsockopt(client_fd, IPPROTO_TCP, TCP_NODELAY, (char*)&flag, sizeof(flag));
    
    char* buffer = new char[100000000]; // 100MB
    memset(buffer, 'A', 100000000);
    
    int total_written = 0;
    char* current_offset = buffer;
    
    // while (total_written < 100000000) {
        int written_now = write(client_fd, current_offset, 100000000 - total_written);
        // if (written_now <= 0) break;
        current_offset += written_now;
        total_written += written_now;
    // }
    
    std::cout << "Server successfully pushed " << total_written << " bytes into the network." << std::endl;
    close(client_fd);
    close(server_fd);
    delete[] buffer;
}

void consumer() {
    int client_fd = socket(AF_INET, SOCK_STREAM, 0);
    struct sockaddr_in address;
    address.sin_family = AF_INET;
    address.sin_port = htons(8080);
    inet_pton(AF_INET, "127.0.0.1", &address.sin_addr);
    
    connect(client_fd, (struct sockaddr *)&address, sizeof(address));
    
    // We want 3 Megabytes
    int requested_bytes = 3000000;
    char* buffer = new char[requested_bytes]; 
    
    std::cout << "Consumer asking OS to read exactly 3,000,000 bytes..." << std::endl;
    
    int total_read = 0;
    char* current_offset = buffer;
    
    while (total_read < requested_bytes) {
        int bytes_read = read(client_fd, current_offset, requested_bytes - total_read);
        if (bytes_read == 0) {
            std::cout << "\n[CONSUMER] read() returned 0! The Server gracefully hung up with a FIN!" << std::endl;
            break;
        }
        if (bytes_read < 0) {
            std::cout << "\n[CONSUMER] read() returned " << bytes_read << "! The Server crashed and fired an RST!" << std::endl;
            break;
        }
        current_offset += bytes_read;
        total_read += bytes_read;
    }
    
    std::cout << "Consumer actually received: " << total_read << " bytes." << std::endl;
    
    close(client_fd);
    delete[] buffer;
}

int main(int argc, char* argv[]) {
    signal(SIGPIPE, SIG_IGN); 
    if (argc < 2) return 1;
    if (strcmp(argv[1], "support") == 0) support();
    if (strcmp(argv[1], "consumer") == 0) consumer();
    return 0;
}
```

  --------
  WE EXPLAIN THE INTRINSIC KERNEL BEHAVIOR

  [ORIGINAL GIT LOG]
  commit 66eec1c6d892a9338dd5e7c74cb8aacf65240f23
  Author: SysDes Admin <pizza@sysdes.local>
  Date:   Wed Jul 15 11:00:49 2026 +0530

  error : Keep drawing at each phase. I need to know what I am doing by seeing a drawing in ASCII before you ask me to do anything.


  [LESSON SYNTHESIS]
  This file is part of the extensive TCP Window Scaling and Blocking Socket debugging journey.
  The experiments in these files proved that blocking write() calls are fundamentally flawed.
  The OS Kernel dictates execution speed by dynamically throttling the TCP Receive Window (cwnd).
  Crucially, via RFC 2140, the Windows Kernel caches these window metrics on the SUBNET/INTERFACE level.
  This means unrelated C++ processes on the same Loopback Subnet silently inherit TCP state from each other.

  [EVIDENCE FROM LESSON 10B]
  --------
  HIDDEN TCP STATE (RFC 2140)

  --------
  WE MEASURE THE SYSTEM BEHAVIOR DIRECTLY

  When benchmarking a server's push capacity across 5 different, brand-new processes connecting to 5 **different** IP addresses (`127.0.0.1` through `127.0.0.5`), the bytes successfully pushed into the non-blocking socket did not reset. Instead, they scaled geometrically:

```text
Run 1 (127.0.0.1): 1,064,960 bytes
Run 2 (127.0.0.2): 1,277,952 bytes (Base + 1 unit)
Run 3 (127.0.0.3): 2,129,920 bytes (Base + 5 units)
Run 4 (127.0.0.4): 1,703,936 bytes (Base + 3 units)
Run 5 (127.0.0.5): 1,277,952 bytes (Base + 1 unit)
```

  Even when rewriting the experiment to use zero threads, zero shared variables, and zero loops (a completely flat script), the results remained identical.

  --------
  VIRTUALLOCK PROOF OF MEMORY LOCKING
  To definitively prove that user-space memory allocation has no effect on the TCP window, we rewrote the C++ program to use `VirtualAlloc` and `VirtualLock`, forcing the OS to lock the 100MB user-space buffer into physical, non-pageable RAM.

  The Kernel ignored the user-space memory architecture completely and applied the exact same temporal route caching heuristics, jumping across strict mathematical "gears" based on a `212,992`-byte interval:

```text
Run 1 (127.0.0.1): 1,064,960 bytes (Baseline)
Run 2 (127.0.0.2): 2,768,896 bytes (+ 8x 212,992)
Run 3 (127.0.0.3): 1,490,944 bytes (+ 2x 212,992)
Run 4 (127.0.0.4): 1,277,952 bytes (+ 1x 212,992)
Run 5 (127.0.0.5): 2,981,888 bytes (+ 9x 212,992)
```

  --------
  WHAT CHANGED IN OUR RUNTIME ENVIRONMENT

  Nothing in the user-space code changed. The operating system kernel intervened.

  --------
  ROOT CAUSE: RFC 2140 AND INTERFACE ROUTE CACHING

  When a C++ program calls `write()` or `send()`, the program does not touch the network. It passes a buffer to the OS Kernel (the `tcpip.sys` driver on Windows), which copies the data into the **Non-Paged Pool**.

  The OS Kernel is a persistent, global entity. It tracks the speed and health of its physical (and virtual) network interfaces to optimize throughput.

  According to **RFC 2140 (TCP Control Block Interdependence)**, modern OS networking stacks deliberately cache TCP Congestion Window (`cwnd`) and Slow Start Threshold (`ssthresh`) metrics. Crucially, Windows caches these metrics against the **Subnet Route** or **Network Interface**, not just the individual IP.

  Because `127.0.0.1`, `127.0.0.2`, and `127.0.0.3` all belong to the `127.0.0.0/8` subnet, they all route through the exact same Virtual Loopback Interface.

  When Run 1 measured zero loss and sub-millisecond ACKs on the loopback interface, the global Kernel updated its heuristic cache for the entire interface. When Run 2 (a brand new C++ process) connected to `127.0.0.2`, the Kernel instantly applied the "warmed up" metrics from Run 1, granting it a massive TCP window. When the consumer application slept for 13 seconds (proving to be a "slow reader"), the Kernel punished the interface, mathematically shrinking the window back down by Runs 4 and 5 to prevent buffer bloat.

  --------
  WE BIND OUR THREADS TO REALITY

  Your C++ program is entirely at the mercy of global Kernel heuristics. Relying on blocking sockets or implicit kernel buffering makes your application's performance unpredictable, as it will inherit the network state left behind by entirely unrelated processes.

  Asynchronous, non-blocking I/O (`poll`, `epoll`, `IOCP`) is the only way to decouple your thread execution from the Kernel's unpredictable TCP scaling algorithms.

  =========================================================================
