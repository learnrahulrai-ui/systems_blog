@title Rahul's Systems Blog
==============================================================================================
  RAHUL'S SYSTEMS BLOG -- systems programming, worked out by hand                     est. 2026
==============================================================================================
  [home](index.txt) | [about](about.txt) | [archive](archive.txt) | [glossary](glossary.txt) | [contact](mailto:learn.rahul.rai@gmail.com)
----------------------------------------------------------------------------------------------

  Plain notes on how systems actually behave when you push them: sockets, epoll, the TCP
  window, mmap, the kernel's non-paged pool. Each note starts from a program that broke,
  reproduces the break, and does not stop until the fix is earned. No hand-waving past the
  hard part, no frameworks, no JavaScript -- just the machine, watched closely.

  Every finding here comes from a real run: the byte counts, the crashes, the error numbers
  are copied from the terminal, not guessed.

  >> START HERE:
     [Most Slow SIMD Is a Slow Mental Model, Not a Slow Chip](posts/simd-slow-brain-not-slow-chip.txt)
     [The Decade in a Flat File: RAM, Disk, and a BSOD](posts/the-decade-in-a-flat-file.txt)
     [The Full Journey: One File, Two Roles, and Every Wall TCP Put in Our Way](posts/tcp-the-full-journey-support-consumer.txt)
     [Support and Consumer: How a Shared File Forced Us Into TCP](posts/support-consumer-shared-file-forced-tcp.txt)
     [TCP: The Whole Contract](posts/tcp-the-whole-contract.txt)
     [TCP Under Pressure: connect() Timeout, Silly Window, and the Blocking Wall](posts/tcp-under-pressure-connect-timeout-sws-blocking-wall.txt)
     [Why a Brand-New Process Inherits Another's Network Speed](posts/inherited-network-speed.txt)

  [Most Slow SIMD Is a Slow Mental Model, Not a Slow Chip](posts/simd-slow-brain-not-slow-chip.txt)
    2026-07-23 . special . simd, avx, matmul, memory-layout, gather, mental-model, c
    A ladder from four bare numbers up to one cell of a matrix product, numbers and arrows
    first, C second, a timed race last. A memorizer's SIMD matrix multiply clocks 7x slower
    than a plain loop and 15x slower than a good one -- identical chip, identical answer. Most
    slow SIMD is a slow mental model, proven by compiling and timing all three.

  [1999: Three Doors Into select() — and the One That Blue-Screens Windows](posts/1999-select-fdsetsize-three-doors.txt)
    2026-07-17 . special . select, fd_set, c10k, windows, bsod, minicom
    Three doors into one event loop -- codebase, happy user, angry user -- and the angry door
    is just 62 ordinary idle connections. Traces the FD_SETSIZE overflow to a real Windows
    stop code, then the O(N^2) trap hiding under the fix.

  [2000: Three Doors Into One Broken Digit](posts/2000-y2k-collision-three-doors.txt)
    2026-07-17 . special . y2k, modulo, cpp, minicom
    Why calling %10 twice on a year can never recover its tens digit, checked in a real
    compiled proof -- then what a missing break statement turns one ID collision into.

  [2001: Three Doors Into a Parser That Was Only Accidentally Correct](posts/2001-accidental-parser-three-doors.txt)
    2026-07-17 . special . parsing, invariants, minicom
    A parser with no bugs in it yet, and the exact unstated assumption that is about to break
    it -- named precisely, before the break happens.

  [2002: Three Doors Into a Read That Misses Its Own Data](posts/2002-stale-index-three-doors.txt)
    2026-07-17 . special . indexing, byte-offset, stale-index, minicom
    A byte-offset index that is correct the instant it is built and wrong the instant the
    first live message lands after it -- traced from the exact tellg() timing to the read
    that silently stops short.

  [2003: Three Doors Into a Data Breach Nobody Coded On Purpose](posts/2003-fragmentation-breach-three-doors.txt)
    2026-07-17 . special . fragmentation, data-breach, hashmap, minicom
    Two ordinary conversations happening at once turns a working parser into a private
    message landing in the wrong person's inbox -- traced line by line, then the
    self-describing-message fix that makes physical file order stop mattering at all.

  [2005: Three Doors Into a Server That Freezes Everyone for One Disk Write](posts/2005-head-of-line-three-doors.txt)
    2026-07-17 . special . head-of-line, event-loop, queue, cdb, minicom
    One thread serving every socket picks up a second job -- the disk -- and a real cdb
    stack trace photographs the whole server standing inside one write. The queue that
    fixes it buys a 512 MB machine exactly 18 minutes of life, arithmetic checked in C++.

  [2005, Part Two: Three Doors Into a Fix That Worked and Froze the UI Anyway](posts/2005-backpressure-choke-three-doors.txt)
    2026-07-17 . special . backpressure, tcp-zero-window, seda, minicom
    Capping the queue stops the flood through four silent dominoes ending at a zero TCP
    window -- and leaves the client's UI saying "Sending..." forever. Matt Welsh's 2001
    SEDA warning, rediscovered one buffer at a time.

  [2006: Three Doors Into Messages That Vanished Holding a Delivery Receipt](posts/2006-detach-data-loss-three-doors.txt)
    2026-07-17 . special . join, detach, graceful-shutdown, data-loss, minicom
    Ctrl+C kills a detached consumer thread mid-write and vaporizes messages the server
    already confirmed as delivered -- join versus detach checked in a compiled proof, then
    the four-piece shutdown that drains the queue before main is allowed to leave.

  [Lesson 1: The Physical Reality of C++ Lambdas](posts/lesson-1-the-physical-reality-of-c-lambdas.txt)
    2026-07-15 . special . concurrency, threads, cpp
    C++ lambdas appear to be magic "inline functions," but structurally, they are nothing more than standard C++ classes generated silently by the compiler.

  [Lesson 2: Threads, Queues, and std::function](posts/lesson-2-threads-queues-and-stdfunction.txt)
    2026-07-15 . special . concurrency, threads, cpp
    If lambdas are just custom classes, how do standard library components like `std::thread` and `std::queue` store and execute them? When you create a worker thread with a lambda: `std::thread` is a template.

  [Lesson 3: The Full Execution Chain (End-to-End)](posts/lesson-3-the-full-execution-chain-end-to-end.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We can now map the complete journey of a single integer (the `client_socket`) from the moment the OS accepts the connection, across the thread boundary, and into the `handleClient` function.

  [Lesson 4: The Fatal Thread Destruction Bug](posts/lesson-4-the-fatal-thread-destruction-bug.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We are using **Bug-Driven Development**. I wrote a standalone experiment `experiment/todo1_shutdown_bug.cpp` that mimics the exact behavior of our `Logger` if you completely ignore **TODO 1** in the destructor.

  [Lesson 5: The Locked Join Deadlock](posts/lesson-5-the-locked-join-deadlock.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You implemented a solution for **TODO 1**, but you left the `join()` call inside the same scope where you locked the mutex: I wrote an experiment `experiment/todo1_locked_join_bug.cpp` to prove exactly what happens mechanically when you execute this.

  [Lesson 6: The Spurious Wakeup Crash](posts/lesson-6-the-spurious-wakeup-crash.txt)
    2026-07-15 . special . concurrency, threads, cpp
    A "Spurious Wakeup" is a well-known hardware/OS anomaly where a thread sleeping on a condition variable suddenly wakes up for absolutely no reason, even if `notify_one()` was never called.

  [Lesson 7: The Flush-on-Shutdown Bug](posts/lesson-7-the-flush-on-shutdown-bug.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You implemented **TODO 4**, but you intentionally omitted the check for `!log_queue_.empty()`. You wrote your wait condition like this: I created `experiment/todo4_stop_only_bug.cpp` using your exact logic.

  [Lesson 8: Total Data Loss (The Power Outage Bug)](posts/lesson-8-total-data-loss-the-power-outage-bug.txt)
    2026-07-15 . special . concurrency, threads, cpp
    Because your lambda only allowed `Logger::processQueue()` to wake up when `stop_` became true, your server was running a "Flush-on-Shutdown" strategy. To simulate what happens when this carelessness meets reality, I built `experiment/todo4_power_o...

  [Lesson 10: The Real-Time Background Worker Proof](posts/lesson-10-the-real-time-background-worker-proof.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You hypothesized that `Logger::processQueue()` only did work when `Logger::~Logger()` (the destructor) was called. To disprove this, I built `experiment/todo4_realtime_proof.cpp` using the fully fixed, perfect Logger design: In `main()`, I did the...

  [Lesson 11: The Truncated Shutdown Bug](posts/lesson-11-the-truncated-shutdown-bug.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You asked what happens if we remove `&& log_queue_.empty()` from the loop termination check: I created `experiment/todo4_truncated_shutdown_bug.cpp` to simulate this.

  [Lesson 12: The Permutation Matrix](posts/lesson-12-the-permutation-matrix.txt)
    2026-07-15 . special . concurrency, threads, cpp
    The Asynchronous Logger relies on exactly 4 variables spread across two lines of code to function correctly: If you remove **any single variable** from these two lines, the server will suffer a catastrophic failure.

  [Lesson 13: Case 1 - The Shutdown Deadlock](posts/lesson-13-case-1---the-shutdown-deadlock.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We removed `stop_` from the condition variable lambda, leaving only the queue check: Your mental trace of the normal execution was **100% flawless**: 1.

  [Lesson 14: Case 2 - The Boolean Inversion Bug](posts/lesson-14-case-2---the-boolean-inversion-bug.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We removed the queue check from the lambda, leaving only the shutdown check: You asked two critical questions and made one massive error in logic. Taking a lock in `log()` is **NOT BAD**.

  [Lesson 15: The Debugger Falls on His Sword (Case 3)](posts/lesson-15-the-debugger-falls-on-his-sword-case-3.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We removed `stop_` from the `if` statement: You traced the normal runtime for a single log: 1. `log("msg")` pushes 1 item and notifies. 2. Background thread wakes up.

  [Lesson 16: Tracing Case 4 (The Truncated Shutdown)](posts/lesson-16-tracing-case-4-the-truncated-shutdown.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We are analyzing Case 4. You removed the queue check from the `if` statement: Let's trace your 4 scenarios line by line to see exactly when this logic fails.

  [Lesson 17: The Ultimate 80-Test Matrix](posts/lesson-17-the-ultimate-80-test-matrix.txt)
    2026-07-15 . special . concurrency, threads, cpp
    I built a C++ test framework that compiled all 8 logic permutations and executed 10 edge-case test cases against each version, capturing the exact point of failure using a timeout supervisor.

  [Lesson 18: The Eternal Server](posts/lesson-18-the-eternal-server.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You asked a brilliant question: *"Servers do not shut down. What happens if we ignore shutdown entirely and just look at the bugs that happen while the server runs indefinitely?"* Let's do the math.

  [Lesson 19: The Out-Of-Memory (OOM) Proof](posts/lesson-19-the-out-of-memory-oom-proof.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You asked for physical C++ proof with no gossip. I wrote `experiment/todo4_missing_consumer_oom_bug.cpp`. In this experiment, I removed `!log_queue_.empty()` from the lambda, leaving only `[this](){ return stop_; }`.

  [Lesson 20: The Immortal Logger](posts/lesson-20-the-immortal-logger.txt)
    2026-07-15 . special . concurrency, threads, cpp
    You theorized that if a program never shuts down (or if you simply do not care about data loss when the laptop crashes), you do not need `stop_` or a `break` clause at all.

  [Lesson 21: Incident 006 — "Marketing Broke Our Server"](posts/lesson-21-incident-006-marketing-broke-our-server.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We are officially graduating from **Correctness Bugs** (deadlocks, race conditions, segfaults) to **Performance Bottlenecks**. Yesterday, our endpoint was fast and everyone was happy: Today, Marketing requested "richer data".

  [Lesson 22: One Request, One Stop — The While Loop Proves It](posts/lesson-22-one-request-one-stop-the-while-loop-proves-it.txt)
    2026-07-15 . special . concurrency, threads, cpp
    **Exactly one request arrives.** Then someone hits cancel/shutdown. No more requests. **1st second — request arrives:** **Shutdown signal arrives:** **Key insight:** The while loop iterates **twice** for one request + one shutdown: File: `experime...

  [Lesson 23: Three Requests, One Stop — The Iteration Count](posts/lesson-23-three-requests-one-stop-the-iteration-count.txt)
    2026-07-15 . special . concurrency, threads, cpp
    For **N requests + 1 stop**, the while loop runs **N + 1** iterations: - **N iterations**: pop each message and add to `pending_` - **1 final iteration**: detect `stop_ && empty` → `flushPending()` → `break` File: `experiment/test_three_req_one_st...

  [Lesson 24: Stop Before Any Log — The Zero Case](posts/lesson-24-stop-before-any-log-the-zero-case.txt)
    2026-07-15 . special . concurrency, threads, cpp
    Server starts. Someone presses stop **before any request arrives**. No log calls. No queue items. No pending buffer. Just a clean shutdown. Will the while loop crash or behave correctly when there's nothing to do?

  [Lesson 25: The Periodic Flush — Three Return Paths](posts/lesson-25-the-periodic-flush-three-return-paths.txt)
    2026-07-15 . special . concurrency, threads, cpp
    Without a timeout, partial `pending_` data sits in RAM until the destructor runs: `cv_.wait(lock, pred)` has **2** return paths: `cv_.wait_for(lock, 5s, pred)` has **3** return paths: The bug: path #3 is unhandled.

  [Lesson 26: The Contract — Why wait and wait_for Are Not Substitutes](posts/lesson-26-the-contract-why-wait-and-waitfor-are-not-substitutes.txt)
    2026-07-15 . special . concurrency, threads, cpp
    We had a working logger. The consumer loop used: We wanted to add a periodic flush every 5 seconds. The obvious change: After 5 seconds of idle, the program crashed on `front()` of an empty queue.

  [Lesson 27: — AssignmentSystem: Industry & Academic Validation](posts/lesson-27-assignmentsystem-industry-academic-validation.txt)
    2026-07-15 . special . concurrency, threads, cpp
    A complete **live chat agent routing engine** inside `mini-server/src/assignment.cpp` and `assignment.h`. **All 5 core logic components are an EXACT MATCH with production systems used by millions of users.** - **What they build:** Thread Pool + Pr...

  The socket-era mistake write-ups that used to sit here as ten standalone stubs now live
  inside the posts that tell their stories: partial writes and the polling wall are in
  [Support and Consumer](posts/support-consumer-shared-file-forced-tcp.txt); TIME_WAIT,
  ghost connections, the silent SIGPIPE death, blocking accept(), the FD leak, SO_LINGER,
  the 212KB read wall, and the write() illusion are all in
  [TCP: The Whole Contract](posts/tcp-the-whole-contract.txt), each with its two-terminal
  demo and the commit that logged it.
